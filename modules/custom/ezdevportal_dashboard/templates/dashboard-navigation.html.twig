{#
/**
 * @file
 */
#}
<div class="dashboard-navigation-main">
    <ul class="dashboard-children">
      {% for key,value in navigationData %}
        <li class="dashboard-menu-item">
        <a href="{{ value.path }}" class=" {{ value.class}}">
          {{ value.text }}
        </a>
             <a href="{{ value.add_icon_path }}" class="add-icon-dashboards {{ value.class}}">
         +
        </a>
        </li>

      {% endfor %}
    </ul>
</div>
